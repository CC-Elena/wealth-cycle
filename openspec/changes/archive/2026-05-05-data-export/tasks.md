# Tasks: 数据导出 (Data Export)

## 后端
- [x] 1. 后端实现 `GovernanceService.exportAllData`。
- [x] 2. 后端新增 `FinanceController` 导出接口。

## 前端
- [x] 3. 前端 `financeStore` 增加导出方法。
- [x] 4. 前端 UI 增加导出按钮并实现下载逻辑。

## 验证
- [x] 5. 验证导出的 JSON 文件格式完整性。
